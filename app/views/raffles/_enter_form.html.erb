<%= form_for entry, url: raffle_entries_path(raffle) do |e| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(entry.errors.count, "error") %> prohibited this post from being saved:</h3>

      <ul>
      <% entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= e.label :comment, 'Enter a comment (optional)' %>
    <%= e.text_field :comment , class:"form-control" %>
    <%= e.submit "Enter", class:"btn btn-success"%>
  </div>
<% end %>